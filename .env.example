# Rename to .env before running. Never commit your real .env to GitHub.
# This is a throwaway example value only - generate your own with:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change_this_to_a_long_random_string_before_using_in_real_project
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
